// DashTest is deprecated — use /dashboard route instead.
// which no longer exists there — companySlice was rewritten to use React Query.
// This test component is now a simple stub; use UpdatedDashboard instead.
const DashTest = () => (
  <div className="p-8 text-gray-500 text-center">
    DashTest is deprecated — use <code>/dashboard</code> route instead.
  </div>
);
export default DashTest;
